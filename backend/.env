# Environment
APP_ENV=local            # or "docker" / "prod"

# Backend selection
DB_ENGINE=mssql          # "mssql" or "sqlite"

# SQL Server / Docker credentials
DB_LOCAL_USER=quant_user
DB_LOCAL_PASSWORD=QuantProject!25
DB_LOCAL_HOST=localhost

DB_DOCKER_USER=sa
DB_DOCKER_PASSWORD=yourpassword
DB_DOCKER_HOST=sqlserver_container_name_or_ip

DB_PORT=1433
DB_INSTANCE=SQLEXPRESS
DB_NAME=QuantDB-v2

# Optional pool tuning
DB_POOL_MIN=1
DB_POOL_MAX=10
